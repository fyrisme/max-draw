{
  "name": "max-draw",
  "version": "0.0.1",
  "author": "fyris",
  "description": "convert max patchers to svg images",
  "type": "module",
  "scripts": {
    "gh-pages-build": "npx vite build converter --emptyOutDir --outDir ../docs --base ./"
  },
  "devDependencies": {
    "@types/node": "^24.0.4"
  },
  "dependencies": {
    "vite": "^7.0.0"
  }
}
